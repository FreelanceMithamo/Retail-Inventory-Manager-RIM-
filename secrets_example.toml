# Copy the lines you need into Streamlit Cloud's "Secrets" box
# (App settings → Secrets), or into a local .streamlit/secrets.toml file
# for testing on your own computer. Never commit real secrets to GitHub —
# this example file only shows the format.

# ---- Option A: WhatsApp via CallMeBot (free) ----
# CALLMEBOT_PHONE = "2547XXXXXXXX"
# CALLMEBOT_APIKEY = "123456"

# ---- Option B: SMS via Africa's Talking (small cost per SMS) ----
# AT_USERNAME = "your_username"
# AT_API_KEY = "your_api_key"
# AT_RECIPIENT = "+2547XXXXXXXX"
